{
  "name": "artarium",
  "version": "1.0.0",
  "description": "歩数×植物育成PWA「名画の庭」のCapacitorネイティブシェル",
  "private": true,
  "type": "module",
  "scripts": {
    "build:www": "node scripts/build-www.mjs",
    "android": "npm run build:www && npx cap sync android && npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/core": "^8.5.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.1"
  }
}
