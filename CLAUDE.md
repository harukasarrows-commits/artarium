# artarium

歩数×植物育成のPWA（Three.js + 自作GLSLシェーダー）。

## デザイン

UIは **「ダークギャラリー」**（2026-07-08 ユーザー決定: 黒基調＋高級感）に従う。正は `styles.css` の `:root` トークン。

- 配色（2026-07-08 ブランド提案書のダークトークンに統一）: 常緑の闇 `#111813` ＋ アイボリー `#edeae0` ＋ 明るい金 `#c2a45e`（押下 `#d8bc78`）。ヘアライン `#2a362d` 系。ティール `#7fa090` は進捗・水面のセカンダリ。緑 `#2e4b3c` は祝福のみ、深紅 `#9f243c` は開花演出・危険のみ。金はCTAと現在地に限定。
- 英語ラベルの基準: 大文字トラッキングの眉ラベル（TODAY'S ARTWORK等）は装飾として使ってよいが、**必ず隣に日本語の見出し・本文を伴う**こと。英語だけで情報を伝えない。
- UXの決まりごと: 名前入力は初回でなく「最初の額装の直前」に聞く。種選択は「カード→開花プレビュー→この種を育てる」の2段階。完成プレートは約2.6秒シーンを見せてから現れる（CSS animation delay）。タップ領域は44px以上。開発用UI（テスト加算・デモパネル）は `?demo=1` のみ。
- 質感の作法: 和文タイトルは明朝＋広い字間、数値はセリフ体（カタログの番号）、ラベルは細身＋字間広め（太字を多用しない）、線は極細ヘアライン `rgba(244,241,232,0.07-0.12)`、カード角丸12px・ヒーロー16px・ボタン8px。
- `DESIGN.md`（クリーム＋コーラル）は**色は不採用**だが、コンポーネント構造・余白・タイポ原則（display=セリフ400＋負トラッキング、hoverを足さない等）は引き続き参照する。
- 3Dシーン（空・水面・植物・額装）の色彩はシーン独自の表現を優先。UIトークンは適用しない。
- **構成の原則（2026-07-08）**: ホームは**没入型** — シーンが画面そのもので、UI（コピー・プレート・ボタン）はその上に浮く。「育てている間は生きた庭、完成したら額装されて美術館（コレクション）へ」という物語の弧に対応する。額縁のメタファーはコレクション側だけに使う。ヒーローを幅100vwにするのは不可（シーンcanvasのリサイズが追随しない）。シェル幅＋100dvhで実装する。
- **新植物の追加時（2026-07-09 ユーザー指示）**: 初期配置は scratchpad の autotune.mjs 相当（直立 rotX/rotZ=0・正面 rotY=-1.57・センタリング・成長カーブB＝目標高さNDC {0.12,0.22,0.36,0.56,0.85,1.25}/幅上限1.9・土 2.5→2.7→2.95→3.2）で自動生成して焼き込む。**その後の微調整はユーザーが手動で行う**ので、自動調整で上書きし直さない。
- **3D配置調整の正（2026-07-09）**: 全植物×全ステージの配置値は `data/model-settings.json`（リポジトリ焼き込み）。起動時に localStorage の設定へマージされる。調整をやり直すときは `?demo=1` の `window.__artariumTune`（get/set/dump）と、ホームビューアの `dataset.plantNdc`（植物のNDC位置計測。土の天面 `soilTopNdcY` も入る）を使う。全植物とも直立・正面向き（plantRotX=0 / plantRotZ=0 / plantRotY=-1.57。旧値 -0.72 は「斜め横向き」でユーザー指摘により変更）。土は soilScale=2.5（旧 3.12 は「大きすぎ」でユーザー指摘により変更）。焼き込みキーは plantScale / plantY / plantRotX / plantRotY / plantRotZ ＋ 土植物のみ soilScale。
- **配置焼き込みの鉄則（2026-07-09 の回帰から）**:
  1. `model-settings.json` には**意図して調整したキーだけ**を入れる（現在: plantScale / plantY / plantRotX / plantRotZ）。`applyBakedModelSettings` はキー単位マージであり、全キー上書きに戻してはいけない — 触っていないパラメータ（土・反射・水面）まで固定化して意図しない見た目変更を生んだ前科がある。
  2. 検証は**クリーンな localStorage だけでなく、旧バージョンの設定が残った状態でも行う**（scratchpad の check-stale-settings.mjs 方式）。実端末には過去の自動保存値が必ず残っている。
  3. 評価基準は「画面に収まる」だけでなく**「姿勢（直立）・接地・土台との比率」**まで見る。花モデルは下方に不可視ジオメトリが伸びていることがあり、バウンディングボックスの底 ≠ 見た目の底。数値合わせのあと必ずスクリーンショットで目視確認する。
  4. 新しいファイルを追加したら **sw.js の CACHE リストへの追加を忘れない**（sedの版数一括更新は既存行しか書き換えない）。
  5. **持続キャンバスを追加したら、シーン再描画の掃除セレクタに除外を追加する**: app.js は再レンダリング時に `canvas:not(.sky-canvas):not(.sky-fx-canvas):not(.water-surface-canvas)` 以外のcanvasを削除する。除外し忘れると初回の再描画で静かに消える（空FXレイヤーで発生した）。
  6. **版数を上げてからデータJSONを書き換えるとSWキャッシュが汚染される**: 版数アップ後に一度でもページを読み込むと（Stopフックの自動オープン含む）、SWインストール時点の内容が新しい `?v=` キーでキャッシュされ、その後のJSON再生成が届かない。対策として `.json` はSWでネットワーク優先にした（sw.js）— これをキャッシュ優先へ戻してはいけない。作業順も「内容の生成が終わってから版数を上げる」を守る。
  7. **検証は check-stale-settings.mjs（旧設定残存＋焼き込み優先の確認）を実値比較で必ずPASSさせてから報告する**。スクリーンショットが良く見えても、キャッシュ・localStorage経路が壊れていると実端末では反映されない。
